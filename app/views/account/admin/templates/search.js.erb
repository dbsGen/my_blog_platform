var render_str = '<%= escape_javascript(render :partial => 'layouts/content',
               :locals => {:partial => 'account/admin/templates/template',
                           :objects => @templates,
                           :page => params[:page],
                           :total_page => @total_page}) %>';
$(render_str).replaceAll('#content-field');