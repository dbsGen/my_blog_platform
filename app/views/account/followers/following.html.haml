= render :partial => 'layouts/content',
         :locals => {:partial => 'account/followers/followers_item',
                     :objects => @users,
                     :page => params[:page],
                     :total_page => @total_page}